export const manipulateAsync = jest.fn(async (uri, actions, options) => ({
  uri: 'mocked-uri',
}));

export const FlipType = {
  Horizontal: 'horizontal',
};

export const SaveFormat = {
  JPEG: 'jpeg',
};
